# Local dev secrets for `wrangler dev`.
# Copy to worker/.dev.vars (gitignored) and fill in — never commit real values.
# For production, set these with `wrangler secret put <NAME>` instead (see worker/README.md).

ANTHROPIC_API_KEY=
LACRM_API_KEY=
EXA_API_KEY=
# PERPLEXITY_API_KEY not needed — D-48's Perplexity code path is dormant/unrouted
