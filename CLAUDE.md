# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

## Project Overview

SalesWhiz (formerly SalesForge, then SynetheixSales D-32, renamed D-35) — a browser-only, screen-reader-first sales dashboard for a non-technical user
(Tim) who navigates entirely via JAWS. Built as a fresh production application from the specs
in `docs/specs/`. The prototype (`salesforge-v2.jsx`) is reference only — do not use its code.

## Tech Stack

- **React 18 + TypeScript 5** via Vite 5
- **react-router-dom v6** with `HashRouter` (hash routing required for GitHub Pages + local
  `file://` use)
- **focus-trap-react v10** (MIT) for dialog focus trapping
- Plain CSS — no framework. All ARIA patterns hand-rolled for full control.

## Development Commands

```bash
# Set NODE_OPTIONS on Windows due to corporate CA cert issue
export NODE_OPTIONS=--use-system-ca

npm run dev          # Vite dev server at http://localhost:5173/
npm run build        # Production build → dist/
npm run preview      # Serve the dist/ build locally
npm run typecheck    # tsc --noEmit (type check without build)
npm run test         # vitest run (unit/integration tests — added M1-T05)
npm install          # May need NODE_OPTIONS=--use-system-ca due to SSL cert issue
```

**Note:** npm registry calls fail with `UNABLE_TO_VERIFY_LEAF_SIGNATURE` unless you prefix
with `NODE_OPTIONS=--use-system-ca`. Add this to your shell profile or run it before npm.

## Architecture

**State:** `src/store/` — `AppStore` interface + `StoreContext` provider. `useLacrmStore`
(M1-T02/T03/T04/T06) is the live implementation: reads leads, pipeline stage, scoring inputs
(score/statusOverride/employees/annualRevenue/industry/dealValue, as LACRM Contact custom
fields), Watchlist pin state (pinned/pinnedNote, also as LACRM Contact custom fields, D-26) and
call history (as LACRM Notes) through from LACRM on mount, and writes contact-field edits, stage
changes, pin/note changes, and logged calls back through the LACRM client. Only `Settings` stays
local-only — it isn't an LACRM concept. Every write goes through `syncWrite()`/`withRetry()`
(`src/utils/retry.ts` — retry/backoff, offline fail-fast) and reverts the optimistic local edit
if it's ultimately unconfirmed ("LACRM wins," PRINCIPLE-01); `AppStore.syncState` tracks global
sync health, shown via `SyncStatusIndicator.tsx` in the header. See
`docs/specs/M1/M1-T02-async-store-swap.md`, `M1-T03-lead-stage-sync.md`,
`M1-T04-extended-state-sync.md`, `M1-T05-conflict-resolution.md`, and
`M1-T06-watchlist-sync-decision.md`. All action methods are `async` (Promise-returning).

**Routing:** Hash-based (`#/`) — `HashRouter` renders `<Routes>` in `<main>`. All 9 sections
are routes; Reports is still a "coming later" honest placeholder from M0. Nurture became a real
page in M7-T01 (D-27) — originally filed as M3-T01, moved to M7 when M3 was reactivated for
Instantly.ai email automation (REQ-05/06); see `docs/specs/M7/M7-00-index.md`. Feedback (D-41)
is the 9th — a standalone request channel, not part of the M0–M7 milestone numbering.

**Accessibility:**
- Skip link → `<header>` → `<nav aria-label="Main navigation">` (links + `aria-current="page"`)
  → `<main id="main-content" tabIndex={-1}>`
- `LiveRegionProvider` in `src/components/LiveRegion.tsx` wraps the whole app and provides the
  `AnnounceContext`. Use `useAnnounce()` to fire polite status messages without stealing focus.
- `FocusTrapDialog` in `src/components/FocusTrapDialog.tsx` — generic focus-trapped dialog shell
  for all modals (T05 lead drawer, T08 call log, etc.). Use `useFocusTrap()` for open/close state.
- `RouteAnnouncer` in `App.tsx` announces the page name on every navigation and moves focus to
  `<main>` on subsequent navigations (not on initial load, so Tab still hits the skip link first).

**Heading structure:** `<h1>SalesWhiz</h1>` in the app header. Each page section opens with
`<h2>` as the first element. Sub-sections within a page use `<h3>`.

## Repo Structure

```
src/
  App.tsx              — skip link, header, AppNav, RouteAnnouncer, Routes
  main.tsx             — React root; provider nesting: HashRouter > LiveRegionProvider > StoreProvider
  index.css            — all global styles (no CSS modules in M0)
  nav/
    AppNav.tsx         — nav landmark + 9 NavItem links with aria-current
  pages/               — one file per section, incl. FeedbackPage.tsx (D-41)
  store/
    types.ts           — Lead, CallLog, Settings, SyncState, NurtureTouch, AppStore interface (async contract)
    lacrmStore.ts      — M1 LACRM-backed implementation of AppStore (read/write-through, retry/revert)
    StoreContext.tsx   — provider + useStore() hook
  nurture/
    nurturePlan.ts     — M7-T01: fixed 4-touch plan, due-date/eligibility helpers (pure, no I/O)
  hooks/
    useAnnounce.ts     — fire to the live region
    useFocusTrap.ts    — open/close state for FocusTrapDialog
    useSpeechToText.ts — D-42: Web Speech API wrapper (Feedback page's "Speak instead")
  types/
    speech-recognition.d.ts — D-42: ambient SpeechRecognition types (not in TS's DOM lib)
  components/
    LiveRegion.tsx     — polite aria-live region + AnnounceContext provider
    FocusTrapDialog.tsx — generic focus-trapped modal shell
    SyncStatusIndicator.tsx — always-visible header badge for AppStore.syncState (M1-T05)
    NurtureTouchDialog.tsx — M7-T01: AI-draft/edit/approve one nurture touch
  utils/
    retry.ts           — withRetry()/isOnline() — retry/backoff + offline detection (M1-T05)
    leadActivity.ts    — isGoneQuiet() — shared "gone quiet" check (Today/All Leads/Nurture)
    feedbackApi.ts     — D-41: submit/list/update-status calls to the Worker's /api/feedback
docs/specs/            — project specifications (master plan + per-milestone files)
worker/                — Cloudflare Worker credential proxy (D-21); separate deploy from the
                          static app, see worker/README.md
```

## Accessibility Rules (PRINCIPLE-03 — applies to every task)

- Every interactive element has a descriptive accessible name.
- Status messages go through `useAnnounce()` — never raw DOM mutations.
- All dialogs use `FocusTrapDialog` — no hand-rolled focus trapping.
- Color is never the only conveyor of meaning — always pair with text.
- List semantics for any list of leads, logs, or options.
- Nav links only; no ARIA tablist for section navigation.

## Key Decisions (record here as they are made)

| # | Decision | Milestone |
|---|----------|-----------|
| D-07 | Browser-only fresh rebuild; prototype is reference only | M0 |
| D-16 | Stack: React 18 + TS 5 + Vite 5 + HashRouter + focus-trap-react | M0-T01 |
| D-17 | Deploy: GitHub Pages (static dist/ from `npm run build`) | M0-T01 |
| D-18 | Store contract is async from the start (all actions return Promise<void>) | M0-T01 |
| D-19 | Section nav uses `<nav>` + `aria-current` links, not ARIA tablist | M0-T01 |
| D-20 | Lead import uses SheetJS (xlsx, Apache 2.0) parsed entirely client-side; no lead data leaves the browser | M0-T02 |
| D-21 | Credential architecture: a Cloudflare Worker (`worker/`) holds the LACRM + Anthropic API keys server-side and exposes two purpose-built endpoints (`/api/anthropic/chat`, `/api/lacrm/ping`); the app calls the Worker, never the upstream APIs directly. First exception to "browser-only, no server" — GitHub Pages deploy (D-17) is unchanged, the Worker is a separate deploy. Free tier ($0 at this traffic scale). See `docs/specs/M1/M1-T00-credential-architecture.md` and `worker/README.md`. | M1-T00 |
| D-22 | Store swap: `useLacrmStore` replaces `useInMemoryStore` behind the unchanged `AppStore` contract. Only LACRM-mapped contact fields (name/company/email/phone/city/state/job title) read/write through today; stage/score/pin-state/call-history/settings stay local-only until T03/T04 wire their sync. `inMemoryStore.ts` deleted as dead code. See `docs/specs/M1/M1-T02-async-store-swap.md`. | M1-T02 |
| D-23 | Pipeline stage sync: `Lead.stage` reads/writes through to LACRM's confirmed sales pipeline via `Pipeline_Items` API functions (`CreatePipelineItem`/`EditPipelineItem`/`GetPipelineItems`, verified against LACRM's real public v2 docs, not guessed). Which of the account's pipelines is "the" sales pipeline is picked by name-overlap against the B-01 confirmed stage list (`selectSalesPipeline()`), since no pipeline *name* was ever confirmed. Added a stage `<select>` to `LeadDrawer` — the only in-app trigger for a stage change — since no prior spec had planned one and T03's acceptance criteria requires the write path to be exercisable. See `docs/specs/M1/M1-T03-lead-stage-sync.md`. | M1-T03 |
| D-24 | Extended-state sync scope, resolved against three conflicts between the T04 spec and the actual codebase (asked Drew rather than guessed, same as B-01): **(1) Nurture** — NurturePage is still M0's placeholder (no touch/approval engine exists), so there's nothing real to sync; B-03 stays open and is deferred to M2, which already scopes building the engine and closing B-03 together. **(2) "Lead notes"** — treated as redundant with call-log notes (already in scope under call history); no new generic Lead.notes field or UI was added. **(3) dealValue** — added to this sync pass (not in T04's literal in-scope list) because the hot-alert acceptance criterion ("consistent across devices/sessions") is unmet without it — the alert filters on dealValue ≥ threshold. Mechanics: score/statusOverride/employees/annualRevenue/industry/dealValue sync as LACRM Contact custom fields (`"SalesForge …"`, bootstrap-created via `CreateCustomField` if absent — `ensureSalesforgeCustomFields()`); call history syncs as one LACRM Note per call (marker-prefixed so other LACRM notes aren't misread as call logs), with `CallLog.id` becoming the real `NoteId` and `Lead.called`/`lastContactDate` derived from call-log dates instead of their own field (so "Mark as called" is now a minimal call log, not a separate patch). Score is *stored*, not purely recomputed on read, because pinnedNote (a scoring input, S-05..S-08) has no LACRM home until T06 — recomputing on every hydrate would silently drop those points each session; local edits still recompute live and push the fresh value back. Hot-alert status and nurture custom fields from the original T01 mapping-doc plan were dropped as unnecessary/out of scope respectively. See `docs/specs/M1/M1-T04-extended-state-sync.md`. | M1-T04 |
| D-25 | Conflict resolution / "LACRM wins" enforcement: fixed a real bug where `updateLead()` dispatched its patch optimistically but never reverted it on write failure — LACRM's actual (unchanged) state didn't win, the stale local edit did. Now every LACRM write goes through a `syncWrite()`/`withRetry()` helper (`src/utils/retry.ts`: 4 attempts, exponential backoff 500ms→8000ms, offline checked before every attempt and fails fast with no retries spent) and, on exhausted failure, `updateLead`/stage edits revert to the last LACRM-confirmed lead snapshot; `importLeads` skips (never fake-adds) a lead whose create call fails. A global `syncState` (`'idle'\|'syncing'\|'offline'\|'error'`, added to `AppStore`) drives a small always-visible header badge (`SyncStatusIndicator.tsx`, text+icon, not a second aria-live region — transitions are already announced once via the existing live-region pattern) and updates instantly on browser `online`/`offline` events, not just on the next failed write. Added `vitest`/`@testing-library/react` (this project's first test runner) with `src/utils/retry.test.ts` and `src/store/lacrmStore.test.ts` proving revert-on-failure, offline fail-fast, and recovery-within-budget against the real hook. See `docs/specs/M1/M1-T05-conflict-resolution.md`. | M1-T05 |
| D-26 | Watchlist sync (open blocker 4, M1-T06): Drew decided pins/private notes **sync to LACRM**, consistent with PRINCIPLE-01, rather than staying an explicit device-local exception. `Lead.pinned`/`pinnedNote` now round-trip as two more LACRM Contact custom fields (`SalesForge Pinned`, `SalesForge Pinned Note`), bootstrap-created the same way as T04's fields via `ensureSalesforgeCustomFields()`. `SalesForge Pinned` is a **Dropdown** (`'Yes'`/`'No'`) rather than LACRM's `Checkbox` type — Checkbox is actually a multi-select-style field with an undocumented value shape, while Dropdown's plain-string in/out was already proven working by `CF_STATUS_OVERRIDE` (T04). No UI change needed: `MyListPage`/`LeadCard`/`useTogglePin` already went through `store.updateLead()`, which now write-throughs and reverts-on-failure for these two fields same as every other synced field (T05). See `docs/specs/M1/M1-T06-watchlist-sync-decision.md`. | M1-T06 |
| D-27 | Nurture engine (closes B-03), originally built as `docs/specs/M3/` rather than a new "M2" folder — the original top-level plan's M2 (`docs/specs/M2-pipeline-nurture-persistence.md`) had REQ-09 (done, M1-T03) + REQ-10/nurture (never built, despite M1-T04/D-24 saying "M2 builds the engine"); the `docs/specs/M2/` folder was later repointed at Clay enrichment (old-M3's scope) instead, silently orphaning REQ-10/B-03 rather than carrying the promise forward. Filed as M3 (the number Drew used asking for it) rather than reopening "M2." **Renumbered to M7 2026-08-10 — see D-28**; spec now at `docs/specs/M7/M7-T01-nurture-engine.md`, code/behavior unchanged. **Built:** `Lead` gains `nurtureEnrolled`/`nurtureEnrolledAt`/`nurtureTouches`/`nurtureArchived` (`src/store/types.ts`), synced as 4 more LACRM Contact custom fields (`CF_NURTURE_*`, `lacrmMapping.ts`) through the same `updateLead()` write-through/revert path every other synced field already uses — no new `AppStore` methods needed. The 4-touch plan (Day 0/7/21/35, email/call) is a fixed pure-function module (`src/nurture/nurturePlan.ts`); a lead is *active* nurture only while `nurtureEnrolled && !nurtureArchived && status === 'Cold'` — computed, not a separate "graduate" write, so a score improvement moves a lead back to the main pipeline with nothing to fall out of sync. `NurturePage.tsx` (replacing M0's placeholder) surfaces gone-quiet Cold leads as enroll-candidates (reusing the same `isGoneQuiet()` check as the existing "Gone Quiet" filter, now deduplicated into `src/utils/leadActivity.ts` from 3 copies), an active-nurture list opening `NurtureTouchDialog.tsx` (AI-draft via the existing `askClaude()` Worker call, same UX pattern as `LeadDrawer`'s opener/next-steps/email tabs, editable, mark done/skip), a "moved back to main pipeline" list, and archive/restore. "Promote to Warm" sets `statusOverride` via the same path every other status change uses — built correctly from the start rather than as a fix, since this rebuild never had the legacy prototype's `promoteModal` bug to begin with. "Approve"/"mark done" means Tim did the touch himself (this app has no send infrastructure — Instantly.ai/Make.com own that, M3/deferred, see D-28); it logs that it happened, same relationship `LeadDrawer`'s copy/mailto actions already have to actual sending. See `docs/specs/M7/M7-T01-nurture-engine.md`. | M7-T01 (orig. M3-T01) |
| D-27a | Custom-field bootstrap loop resilience: `ensureSalesforgeCustomFields()` (`lacrmStore.ts`) attempted every missing field in one sequential loop with no per-iteration error handling — one rejected `createCustomField()` call threw and silently killed every field after it in `SALESFORGE_CUSTOM_FIELDS`, forever, every hydrate. Found live 2026-08-10 verifying M7-T01: **9 of 13 SalesForge custom fields had never actually been created** in the real production account, including fields from M1-T04/M1-T06 already marked done. Fixed with a per-field `try/catch` inside the loop, so one bad spec can't block unrelated fields. See `docs/specs/M7/M7-T01-nurture-engine.md`. | M7-T01 (found; orig. M3-T01) |
| D-27b | `Currency`-type custom fields need `CurrencyDisplaySettings` — an array of exactly one `{CurrencyType, CurrencySymbol, NumberOfDecimalPlaces, SymbolPlacement}` object (confirmed live against LACRM's real API, not the docs' prose alone: a plain object was tried first and also rejected with a 400). `CF_ANNUAL_REVENUE`/`CF_DEAL_VALUE` (M1-T04) were missing this entirely — the actual trigger for D-27a's cascading failure. Fixed in `lacrmMapping.ts`/`lacrmApi.ts`; both fields now create successfully. | M7-T01 (found; orig. M3-T01) |
| D-27c | `Lead.importedAt` was `new Date().toISOString()` — "now" — on every single hydrate (`contactToLead()`, `lacrmStore.ts`), not the lead's actual import date. `isGoneQuiet()` falls back to `importedAt` whenever `lastContactDate` is null (true for virtually every real lead, since call-logging through this app is new) — resetting it to "now" every load meant no lead could *ever* register as gone-quiet against real data. Silently broken since M1-T04; the existing "Gone Quiet" filter (All Leads) and Today's gone-quiet notice were both affected, not just Nurture's eligibility list. Fixed by mapping LACRM's real `DateCreated` field (confirmed present on `GetContacts`/`GetContact` responses) instead. Verified live: Gone Quiet went from 0/21,212 to 21,194/21,212 — correct for a bulk-imported prospect list with no call history yet. | M7-T01 (found; orig. M3-T01) |
| D-28 | M3 (Instantly.ai email automation, REQ-05/06) was briefly reactivated 2026-08-10 — D-03/D-04's "deferred indefinitely" was read as a status note, not a rescinded decision (Drew's call at the time, D-14 authority), and 4 real task specs (T00-T03) were drafted. This reclaimed the M3 folder from D-27's nurture engine, which had no other number reserved for it; rather than delete real, shipped, live-verified code's documentation (`NurturePage.tsx`, `nurtureTouches`, 4 live LACRM custom fields — D-27/a/b/c), the nurture-engine spec moved to `docs/specs/M7/` (next open number) with references updated, no code changed. **Reversed 2026-08-11**, after Drew talked it through with his mentor: M3/Instantly.ai goes back to deferred, not active — same status D-04 originally gave it, door left open to revisit later. The T00-T03 specs stay in `docs/specs/M3/` untouched as dormant reference, not deleted. The M7 move for the nurture engine **stands** — moving it back would just be more churn, and M7 is now its correct permanent home regardless of M3's status. `M4-outreach-alerts-deferred.md` needed no edit either way — its "deferred indefinitely" framing was right before the reactivation and is right again now. See `docs/specs/M3/M3-00-index.md` ("Status: parked, not active") and `docs/specs/M7/M7-00-index.md`. | M3 reactivation, then reversal |
| D-29 | Tim's live JAWS test (2026-08-12) confirmed the app works well overall, with a small set of requested changes. First one: **remove the first-run onboarding tour** (`OnboardingTour.tsx`, `src/content/onboarding.ts`, built under M0-T10/gap G4). Deleted both files; stripped tour state/effect/`openTour` from `HelpProvider.tsx` and `HelpContextValue`, the "Take the app tour" button and `onOpenTour` prop from `GlossaryDialog.tsx`, and the now-unused `.onboarding-progress`/`.onboarding-actions`/`.glossary-tour-link` CSS. The explanation system and glossary (also part of M0-T10) are unaffected — Tim's feedback was about the tour specifically, not plain-language help generally. `docs/specs/M0/M0-T10-plain-language-onboarding.md` struck the tour bullet/acceptance criteria rather than being silently out of sync with shipped code. | Post-M0 (Tim JAWS feedback) |
| D-30 | Second item from Tim's JAWS feedback (2026-08-12): let the AI assistant (`AskAiDialog.tsx`, M0-T11) navigate straight to a contact/lead instead of only answering questions about them. Added a "Find a lead" search (`src/utils/leadSearch.ts` — `findLeads()`) above the existing question box, in the same dialog: type a company or contact name, get back up to 8 ranked matches (exact → starts-with → contains) as a button list, select one to navigate there. Deliberately **not** an LLM call for the matching itself — local substring/rank matching against already-loaded `store.leads` is instant, free, and can't hallucinate a lead into existing; the existing Anthropic-backed Q&A flow is untouched and still the only part of this dialog that calls `askClaude()`. Navigation mechanics: `navigate('/all-leads', { state: { openLeadId } })` (All Leads is the only list holding every lead regardless of status/filter) — `AllLeadsPage` reads `location.state.openLeadId` in a `useEffect` once `loading.leads` is false, opens that lead's `LeadDrawer`, then clears the nav state via `navigate(..., { replace: true, state: null })` so revisiting the page later doesn't reopen it. Read-only UI action, no lead data mutated — consistent with M0-T11's existing "answers; doesn't mutate data" scope line, no scope conflict. See `docs/specs/M0/M0-T11-ai-assistant.md`. | Post-M0 (Tim JAWS feedback) |
| D-31 | Third item, same session: "find the guy from this city" — extended `findLeads()` to also match `Lead.city`/`Lead.state`, and to match a whole typed phrase, not just an exact field value. Added reverse containment (`query.includes(field)`, not just `field.includes(query)`) so "the guy from dallas" matches a lead whose city is "Dallas" without Tim needing to type just the city name alone; guarded to fields ≥3 chars so a bare 2-letter state code can't spuriously match inside an unrelated word (e.g. "context" contains "tx"). State also matches by full name ("Texas") even though leads store the abbreviation, via a new `stateFullName()` reverse lookup added to `usGeo.ts` (built off the state-name table `geocodeLead()`/Map page already had — reused, not duplicated) — and by the 2-letter code itself, but only as a whole word in the typed query (token match), not a raw substring. `findLeads()` now returns `{ matches, totalMatches }` instead of a bare array so the UI can say "showing top 8 of N" when a city match is broad (real account has ~21,212 leads — a city could plausibly return more than 8); results additionally tie-broken by lead score (hottest first) so the most useful match surfaces when many leads share a city. `src/utils/leadSearch.test.ts` added (9 cases) covering company/contact/city/state matching, the natural-phrase case, and the false-positive guard. See `docs/specs/M0/M0-T11-ai-assistant.md`. | Post-M0 (Tim JAWS feedback) |
| D-32 | Product renamed **SalesForge → SynetheixSales** (Drew's request, citing trademark/copyright exposure from the closeness to "Salesforce"). Scope was explicitly negotiated, not assumed: (1) **user-facing display** — `<title>`, header `<h1>`, `PAGE_TITLES` fallback, the AI assistant's persona prompt text, glossary intro copy, `package.json` name (`salesforge` → `synetheixsales`, lockfile regenerated), `CLAUDE.md`'s project overview/heading-structure lines — all renamed. (2) **Deployed Cloudflare Worker** — `wrangler.toml` name `salesforge-api` → `synetheixsales-api`. Renaming and redeploying created a *new* Worker at a new URL (`https://synetheixsales-api.salesforge.workers.dev` — the `salesforge` segment there is the Cloudflare account's shared `workers.dev` subdomain, an account-wide setting, not part of this rename) rather than renaming the live one in place, so the old Worker kept running untouched throughout — nothing broke automatically. **Cutover completed 2026-08-12**: Drew ran `npx wrangler deploy` (already authenticated locally) and set both secrets (`ANTHROPIC_API_KEY`, `LACRM_API_KEY`, same values as the old Worker — pasted directly into his own terminal via `wrangler secret put`, never through chat) on the new Worker; verified end-to-end via `/api/lacrm/ping` (returned real account data). The `WORKER_URL` GitHub Actions repo variable was then updated and `deploy.yml` re-run via `workflow_dispatch`; confirmed live by fetching the deployed JS bundle and finding the new Worker URL baked in. Old `salesforge-api` Worker left running (not yet deleted) as a rollback safety net — delete once the new one's been stable a while. (3) **Initially left unchanged, then migrated — see D-33**: the 13 live LACRM Contact custom-field names and the call-log note marker. | Post-M0 (Tim's copyright-exposure request) |
| D-33 | Completes D-32: Drew decided the copyright/trademark concern reached the private LACRM field labels too, not just the public-facing product. Once the D-32 Worker cutover gave a working credential path, live-checked LACRM's real API docs rather than assuming — confirmed `EditCustomField` (rename a field by `CustomFieldId`, same field/data, just a new `Name`) and `EditNote` (rewrite a note's text by `NoteId`) both exist. That turned this from "can't be done without risking the 21,212-contact account's already-synced data" into a safe in-place migration. Mechanics: temporarily added `PATCH /api/lacrm/custom-fields/:id` and `PATCH /api/lacrm/notes/:id` to the Worker (`worker/src/index.ts`), ran a one-off Node script (`scratchpad/migrate-rename.mjs`, not committed — throwaway) that renamed all 13 `CF_*` fields and would've rewritten any `"SalesForge Call Log"`-marked notes (found 0 — call logging hasn't been used in production yet), then reverted the Worker routes (net diff zero) and redeployed. **Real gotcha hit live**: `EditCustomField` treats an omitted `Options`/`CurrencyDisplaySettings` as "clear it," not "leave unchanged" — same class of surprise as D-27b's Currency bug. The first test call (Nurture Archived) wiped its `Yes`/`No` options; caught immediately by re-fetching the field afterward (a habit, not luck — verify after every live-data write, per PRINCIPLE-01's spirit), fixed by restoring them, and every subsequent field-rename call included the field's existing `Options` (with `OptionId`s preserved, not regenerated) and `CurrencyDisplaySettings`. Verified live: `Status Override` kept its `Hot`/`Warm`/`Cold` options; a sample of 2,500+ real contacts confirmed no per-lead data was actually at risk (this account's engagement fields — pin, nurture — are almost entirely unset so far, consistent with D-27c's finding that it's a fresh bulk import); `ping` and a live contact fetch both confirmed correct reads under the new field names post-migration. `CF_*` constants (`lacrmMapping.ts`) and the matching `LacrmSalesforgeCustomFields` interface keys (`lacrmApi.ts`) updated to match, deployed immediately after the live rename to close the mismatch window (briefly, the deployed app was looking for field names that no longer existed — closed same session, not left open). See the note atop `lacrmMapping.ts`. | Post-M0 (Tim's copyright-exposure request) |
| D-34 | Fourth item from Tim's JAWS feedback: replaced the visual US map (M0-T06, gap G3) with filtering — Tim's actual need was knowing whether it's a good time to call someone in a different time zone, and a map never told him that. Deleted `MapPage.tsx`/`USAMap.tsx`; added `CallTimesPage.tsx` at `/call-times` (nav label "Call Times", replacing "Map"). Kept T06's status/city filters, added state and time-zone filters (time-zone options built from the distinct zones actually present among leads, labeled with each zone's current abbreviation via the existing `getTZInfo()`, TZ-01), and added a "Good calling hours only" toggle that filters to leads where `!getTZInfo(lead.timezone, now).badHour` — the direct answer to "can I call them right now." `LeadCard`'s existing per-lead local-time/calling-hours display (TZ-01) is unchanged and still does the per-card work; this page's new job is narrowing the list down first. Removed now-unused map dependencies (`d3-geo`, `topojson-client`, `us-atlas` + their `@types`) — gzipped bundle dropped from 248.72 KB to 205.69 KB. `usGeo.ts` trimmed to keep only `normalizeStateAbbr()`/`stateFullName()` (still used by D-31's lead search) and drop `geocodeLead()`/coordinate tables (map-only). Worth noting: M0-T06's own accessibility constraint had the map `aria-hidden` and decorative-only from the start, with an accessible list as the required non-map equivalent — Tim, JAWS-only, never actually perceived the map visually anyway, so this removes dead weight rather than a feature he used. See `docs/specs/M0/M0-T06-map-view.md`. | Post-M0 (Tim JAWS feedback) |
| D-35 | Product renamed again, **SynetheixSales → SalesWhiz** (Drew's request, 2026-08-13; no reason given this time, unlike D-32's trademark concern). Scoped narrower than D-32 on purpose — Drew explicitly chose "code + docs only" over also touching the deployed Worker or live LACRM field names this round. **Renamed:** all user-facing display text (`<title>`, header `<h1>`, `PAGE_TITLES` fallback, AI assistant persona prompt, glossary intro copy), `package.json`/`package-lock.json` name (`synetheixsales` → `saleswhiz`), prose comments throughout `src/` (`retry.ts`, `types.ts`, `lacrmStore.ts`, `lacrmApi.ts`, `lacrmMapping.ts` header), this file's project-overview/heading-structure lines, and every `docs/specs/**/*.md` reference (26 files). **Deliberately left unchanged:** the 13 live LACRM Contact custom-field names and the call-log note marker (`CF_*` constants and `CALL_LOG_NOTE_MARKER` in `lacrmMapping.ts`, and the matching `LacrmSalesforgeCustomFields` interface keys in `lacrmApi.ts` — all still literally `"SynetheixSales …"`, flagged with an inline note so a future pass doesn't blind-rename them) and the deployed Cloudflare Worker (`wrangler.toml` name still `synetheixsales-api`, its live URL, and `worker/` prose comments referring to it by that name) — both would need the same kind of live migration D-33 did (`EditCustomField`/`EditNote` for the fields, a new `wrangler deploy` + re-pasted secrets + `WORKER_URL` cutover for the Worker), which needs Drew's own authenticated terminal for secrets, not something to do silently as a side effect of a display-name change. If the Worker/LACRM-field rename is wanted later, follow D-32/D-33's playbook, targeting SalesWhiz instead of SynetheixSales. | Post-M0 (Drew's request) |
| D-36 | The deployed Worker's `LACRM_API_KEY` secret switched accounts: **Greg Siebert → Tim Dumler** (2026-08-18) — Tim is the actual end user this app is built for (PRINCIPLE-01, JAWS testing, etc.); Greg's key had been in place from an earlier setup/testing pass and was never a deliberate "whose account" decision on record. Set via the Cloudflare dashboard (Workers & Pages → `synetheixsales-api` → Settings → Variables and Secrets → edit `LACRM_API_KEY` → Save and deploy) rather than `wrangler secret put`, which was crashing on Windows (`Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)`, libuv, in Wrangler's interactive-prompt cleanup path — a local environment issue, not a real failure of the secret write itself; piping the value non-interactively, e.g. `"key" \| npx wrangler secret put LACRM_API_KEY`, is the terminal-based workaround if needed again). Verified live via `/api/lacrm/ping`: went from `{"FirstName":"Greg","LastName":"Siebert",...}` to `{"FirstName":"Tim","LastName":"Dumler","Email":"tim.dumler@proforma.com",...}`. Considered and explicitly declined: dual-account access (either a manual-swap-only or a per-session account-switcher) — Drew confirmed the app should just run against Tim's account, not both. Scope stayed credential-only: no code changed, since which LACRM account the Worker talks to was never hard-coded anywhere in `src/`. | Post-M0 (Drew's request) |
| D-37 | M5-T00a resolved (2026-08-18): with M4/Instantly.ai staying deferred (D-28), REQ-11's two Instantly-dependent metrics — **emails sent** and **response rate** — get no data source. Drew's call: **placeholder, not drop or proxy**. Both stay in REQ-11's metric list and render as an explicit "pending" state in the eventual dashboard (M5-T02), consistent with this project's established deferred-features-stay-visible pattern (M4 itself). No manual/proxy substitute considered good enough to stand in for either metric. `M5-T00a-metric-reconciliation.md` and `M5-reporting.md`'s B-06 candidate-metrics table updated to match. This unblocks **M5-T00** (Tim's gap-analysis session, "what do you wish you could see that you can't get from the app today?") — no longer blocked on T00a, and now genuinely actionable since Tim is a live daily user of the app as of D-36. Chosen over scoping M6 (Operations) next: M6's own gate (M1–M5 live in production) isn't met — M2 (Clay) is still blocked on Drew's credit-guard call and M5 is still M0's placeholder — so M5 is the real next milestone, not M6. | M5-T00a |
| D-40 | Reports' AI budget section reframed as **"Monthly Spending"** — Drew asked to see "how much we're spending for everything each month." Two follow-up questions narrowed this rather than guessing: (1) Drew considered, then declined, pulling Anthropic's official Usage & Cost Admin API in alongside the existing real-time figure — that API needs an Organization-tier Anthropic account (unconfirmed whether Tim's account qualifies) plus a new elevated Admin API key, and its data lags ~5 minutes, so it couldn't strengthen the actual $20 cap (D-39) already enforced in real time from the same per-call token counts; not worth the setup for a number that's purely a slower echo of what's already shown. (2) Scope is **usage-based costs only** — LACRM's flat subscription fee stays out (it doesn't change with app usage, and there's no API to read it from; a manual Settings entry was considered and declined). Since the AI assistant is still the only live usage-based cost, the dollar figures shown are unchanged from D-39 — this is a copy/framing change, not a new cost source: heading and stat label ("Spent this month" → "Total spent this month"), an intro line stating the usage-based-only scope, and a note that Clay enrichment credits (M2, still blocked) would become a second line item here once that ships, rather than needing another rename. Glossary entry renamed `ai-cost-budget` → `monthly-spending` to match; internal code (variable/CSS names, the Worker's KV tracking) intentionally left as `ai-budget-*`/D-39's mechanics, unchanged. | Post-M5 (Drew's request) |
| D-39 | AI cost budget monitor, requested by Drew: SalesWhiz's only usage-scaling cost is the Anthropic API (the AI assistant's Q&A and nurture-draft calls, via the Worker's `/api/anthropic/chat`, model `claude-haiku-4-5`) — LACRM is a flat subscription and Cloudflare's Workers free tier doesn't vary with this app's traffic, so neither needed monitoring. Drew set the cap at **$20/month**, enforced (not just displayed) — confirmed via clarifying questions rather than guessed, since both the dollar figure and "warn vs. hard-block" were genuinely his call. Enforcement had to be server-side: a client-only guard can't cap spend across devices/sessions, so the Worker now tracks running monthly cost in a new Cloudflare Workers KV namespace (`AI_COST_KV`) and checks it before every Anthropic call, returning `402` once the cap is hit — the AI assistant pauses itself until the next calendar month while the rest of the app (leads, pipeline, nurture, reports) keeps working normally, since only the AI features were ever in scope. Cost per call is computed from the real `usage.input_tokens`/`usage.output_tokens` Anthropic returns, priced at Haiku 4.5's public per-token rate ($1.00/$5.00 per MTok in/out). `GET /api/anthropic/usage` exposes the running total; `ReportsPage.tsx` adds an "AI Assistant Budget" section (spent/budget/remaining, plus a status badge at 80%/100% thresholds using the same icon+text-never-color-alone pattern as `SyncStatusIndicator.tsx`, PRINCIPLE-03) — Reports was the natural home since D-38 already made it the live-metrics dashboard. KV has no atomic increment, so concurrent requests could race and undercount by a call or two; accepted as a real tradeoff given this app's actual usage (one user, a handful of AI calls a day) rather than engineered around. Needs one manual step from Drew before it's live: `npx wrangler kv namespace create AI_COST_KV` and pasting the id into `wrangler.toml` (same class of one-time setup as the Worker's original secrets, see `worker/README.md`) — until then `/api/anthropic/chat`/`/usage` fail closed with no effect on the rest of the app. | Post-M5 (Drew's request) |
| D-38 | M5-T01/T02 built as a v0 (2026-08-18), deliberately ahead of T00 — Drew's call to ship "the basics we have" now rather than wait for Tim's actual gap-analysis session, and revise once that happens. `ReportsPage.tsx` (replacing M0's placeholder) shows a live "Pipeline Snapshot" — total leads, leads qualified (reuses the existing `scoreQualificationThreshold` Settings concept, not a new definition), sample boxes sent (leads at/past the confirmed "Sample Box Sent" stage) — plus an honest "Not Yet Available" list for the other four approved-scope metrics (cost per lead, alerts triggered, emails sent, response rate), styled with the existing `.interim-badge`, not a fabricated zero. Real finding from T01: LACRM stores only a contact's *current* pipeline stage, never a history of stage changes, so "boxes sent" (and any stage-based metric) can only ever be a live snapshot, not a true monthly count of a send event — the UI copy says so explicitly rather than implying a monthly figure it can't actually produce. Same finding for "alerts triggered": REQ-07's hot-alert (`TodayPage.tsx`) computes live and was never logged anywhere (D-24), so there's no history to count. Metric logic is pure/tested (`src/utils/reportingMetrics.ts` + test file), no new parallel data store — every real number traces to already-synced `Lead` fields (PRINCIPLE-01). See `M5-T01-data-source-mapping.md`/`M5-T02-reporting-dashboard-ui.md` "Decision & what was built," and `M5-00-index.md`'s status note that this isn't M5's final scope. | M5-T01/T02 (v0) |
| D-41 | New **Feedback** nav page (`/feedback`, `FeedbackPage.tsx`) — a standalone request channel Drew asked for so Tim can type anything he thinks would help and Drew can see it and update the app, without needing a live conversation. Scoped via two quick questions rather than guessed: **placement** is a dedicated nav page, not a header dialog or a box tucked into an existing page — big enough for a running list of requests, unlike the transient Ask AI dialog; **notification** is in-app only (Drew checks the page), not real-time email/Slack — avoids a new external credential (e.g. Resend/SendGrid) for a single-user app where Drew can just open the page. Mechanics: not an LACRM concept (like Settings), so it doesn't go through `AppStore`/`useLacrmStore` — instead a new Worker KV namespace (`FEEDBACK_KV`, same one-time-setup pattern as D-39's `AI_COST_KV`) backs three endpoints (`POST/GET /api/feedback`, `PATCH /api/feedback/:id`) called from `src/utils/feedbackApi.ts`. Each entry is `{id, text, submittedAt, status: 'new'|'reviewed'}`; the page shows a textarea to submit plus a list of past requests with a "Mark reviewed"/"Mark new" toggle, so Drew has a lightweight way to track what he's acted on. Needs the same one-time manual step as D-39 before it's live: `npx wrangler kv namespace create FEEDBACK_KV` and pasting the id into `wrangler.toml` (see `worker/README.md`) — until then `/api/feedback` fails closed with no effect on the rest of the app. In passing, fixed a stale bug found while editing `AppNav.tsx`/`App.tsx`: the `/call-times` nav label and announced page title were still "Map", left over from before D-34 renamed the page — now "Call Times" as D-34 actually specified. | Post-M5 (Drew's request) |
| D-43 | The `/call-times` nav label and page title, "Call Times" since D-41's incidental bug fix (it found the label still said "Map" and corrected it toward D-34's original decision), reverted back to **"Map"** — Drew's explicit call: that's the name Tim already knows the page by, so matching his habit wins over matching D-34's on-paper name. Only `AppNav.tsx`/`App.tsx`'s label/title changed; `CallTimesPage.tsx`'s own `<h2>` heading was never touched by D-41 in the first place and has said "Map" all along, so this actually *resolves* an inconsistency D-41 introduced (nav said one thing, the page's own heading said another) rather than reopening one. The underlying page (filters, no visual map — D-34) is unchanged; only the label reverts. | Post-M5 (Drew's request) |
| D-44 | Real bug found live 2026-08-19/20: asking the AI assistant a question with **"All leads"** scope against the real ~21,212-lead production account failed — `280498 tokens > 200000 maximum`, Anthropic's hard context-window ceiling. `summarizeLead()`-ing every lead in scope had no cap, so any scope large enough (in practice, "All leads" on this account; "Hot and Warm" would hit it too once there are enough Hot/Warm leads) silently built an oversized prompt with no warning until the API call failed. Fixed with `selectLeadsForPrompt()` (new `src/utils/askAiPrompt.ts`, extracted from `AskAiDialog.tsx` so it's unit-testable): caps what actually goes in the prompt at `MAX_LEADS_IN_PROMPT = 300`, sorted by score descending so the highest-value leads survive the cut — comfortably safe at any real account size (300 leads is ~4-6k tokens, nowhere near the 200k ceiling, even before counting the rest of the prompt). When a scope is truncated, both the model and Tim/Drew are told honestly rather than silently: the prompt itself says not every lead in scope is listed, and a visible note under the scope selector states the real count and that only the top `MAX_LEADS_IN_PROMPT` are included — same "showing top N of M" honesty pattern D-31's `findLeads()` already established. This is a separate limit from D-39's $20/month spend cap: that's still enforced server-side regardless of this fix, but capping prompt size also keeps typical per-call cost small and predictable, which helps the monthly budget stretch further as a side benefit. `src/utils/askAiPrompt.test.ts` covers the cap, the score-sort-on-truncation, and real-account-scale (21,212 leads) not exceeding the cap. | Post-M5 (found live) |
| D-45 | AI assistant's "Find a lead" search (D-30/D-31) extended to match by **industry** — Drew asked, then clarified there's no NAICS code system behind `Lead.industry`, just whatever free text ended up in the LACRM custom field (`CF_INDUSTRY`). That turned out not to matter: `findLeads()` already does plain substring/reverse-substring matching for city and state (D-31), and industry needed nothing more — "find restaurants" matches a lead whose industry field literally says "Restaurants" the same way "the guy from dallas" matches on city, no classification/lookup table required. One line added to `leadMatchScore()` (`src/utils/leadSearch.ts`); leads with no industry set are skipped rather than false-matching on an empty string. `src/utils/leadSearch.test.ts` covers a direct industry match, a natural phrase ("someone in healthcare"), and the no-industry-set case. | Post-M5 (Drew's request) |
| D-46 | AI assistant's "Ask a question" (`AskAiDialog.tsx`) can now target **a specific lead** instead of only a status-based group — Drew's request. Added as a 4th scope option ("A specific lead") alongside Hot/Hot+Warm/All; picking it reveals a small search-and-pick widget (reuses `findLeads()`, same matching as "Find a lead" above it in the same dialog, but selects a target for the question instead of navigating away) showing "Asking about {company}" once chosen, with a "Change lead" control. The Ask button is disabled until both a lead is picked and a question is typed. Swaps in lead-specific starter questions ("What's the best way to approach this lead?") instead of the group-oriented ones. Reuses D-44's `selectLeadsForPrompt()`/`summarizeLead()` infrastructure unchanged — a 1-lead scope is trivially under the token cap, so this is essentially free from a token/budget standpoint (verified live: a real single-lead question sent a 517-character prompt and returned a correct, on-topic answer). Same PII boundary as every other scope (no email/phone/pinnedNote sent). No new tests needed — this reuses `findLeads()` (D-30/D-31/D-45, already tested) and `selectLeadsForPrompt()`/`summarizeLead()` (D-44, already tested) as-is; verified instead via a live end-to-end Playwright run (pick lead → ask → real answer from the real Worker) rather than duplicating coverage of logic that isn't new. | Post-M5 (Drew's request) |
| D-42 | Voice input added to the Feedback page (D-41) — Drew's follow-up request so Tim can speak a request instead of typing it. Uses the browser's built-in Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`), not a Worker call — no new credential, consistent with keeping this feature dependency-light. New `src/hooks/useSpeechToText.ts` (ambient types in `src/types/speech-recognition.d.ts`, since the API isn't in TypeScript's standard DOM lib) wraps start/stop/error handling; a "Speak instead" button next to the textarea toggles listening, appends the transcript to whatever's already typed (so voice and typing can mix), and announces state changes ("Listening…", "Got it.", "Stopped listening.") via `useAnnounce()` since JAWS has no way to see a visual "recording" indicator. The button only renders when `useSpeechToText().supported` is true — Chromium browsers (Chrome, Edge) support the API, Firefox doesn't, so it degrades to no button rather than a dead one. Worth knowing: Chrome's implementation sends the raw audio to Google's servers to transcribe it — that's the browser's own behavior, not something this app controls or routes through the Worker. | Post-M5 (Drew's request) |
