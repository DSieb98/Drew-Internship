# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

<!-- Describe what this project does and its purpose -->

## Tech Stack

<!-- List primary language(s), frameworks, and key dependencies -->

## Development Commands

<!-- Add commands once the project is set up, e.g.: -->
<!-- Build: `npm run build` / `make build` / `python -m build` -->
<!-- Test: `npm test` / `pytest` / `go test ./...` -->
<!-- Lint: `npm run lint` / `ruff check .` / `golangci-lint run` -->
<!-- Dev server: `npm run dev` / `uvicorn main:app --reload` -->

## Architecture

<!-- Describe the high-level structure once code exists:
- Major modules/layers and their responsibilities
- How data flows through the system
- Key design decisions or patterns in use
-->

## Repo Structure

<!-- Fill in once directories are established, e.g.:
- `src/` — application source code
- `docs/specs/` — project specifications
- `tests/` — test suite
-->
